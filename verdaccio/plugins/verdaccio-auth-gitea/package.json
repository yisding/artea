{
  "name": "verdaccio-auth-gitea",
  "version": "0.1.0",
  "private": true,
  "description": "Verdaccio auth plugin: validates user:PAT via HTTP Basic against Gitea and maps org membership to Verdaccio groups",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@verdaccio/types": "^13.0.1",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  }
}
