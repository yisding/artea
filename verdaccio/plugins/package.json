{
  "name": "artea-verdaccio-plugins",
  "version": "0.1.0",
  "private": true,
  "description": "pnpm workspace for Artea's Verdaccio plugins (build here, mount the whole directory into the verdaccio container)",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  }
}
